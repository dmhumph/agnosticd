apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
  - name: {{ htpasswd_provider_name }}
    mappingMethod: claim
    type: "HTPasswd"
    htpasswd:
      fileData:
        name: {{ htpasswd_secert_name }}